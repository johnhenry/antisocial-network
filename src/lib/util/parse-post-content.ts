import { Agent } from "@/types/mod";
const parsePostContent = (content: string, mentions:Agent[], tools:string[]): string => {
  //TODO: Parse content
  return content;
}

export default parsePostContent;